# 📘 项目介绍
`ToolDelta` 是依赖于 `PhoenixBuilder | NeOmega | FateArk` 等接入点的多功能扩展组件，可以加载各种各样有趣的插件供租赁服使用。

## 注意事项
- 项目目前可完全在 `Ubuntu / Windows` 平台上流畅部署，可在 `Ubuntu / Windows / Termux` 上运行，遇到平台不兼容的情况请尽快提交 `Issue`
- 在 `Termux` 上运行若出现任何问题也请尽快提交 `Issue`
